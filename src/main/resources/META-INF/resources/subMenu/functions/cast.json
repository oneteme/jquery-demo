[
  {
    "title": "VARCHAR",
    "view": "products",
    "column": "price,price.varchar(2):PriceToVarchar",
    "definition": "Changes column type to VARCHAR",
    "syntax": "column.varchar(size) / varchar(column,size)"
  },
  {
    "title": "INTEGER",
    "view": "products",
    "column": "price,price.integer:PriceToInteger",
    "definition": "Converts column type to INTEGER",
    "syntax": "column.integer / integer(column)"
  },
  {
    "title": "BIG INTEGER",
    "view": "products",
    "column": "price,price.bigint:PriceToBigint",
    "definition": "Converts column type to BIG INTEGER",
    "syntax": "column.bigint / bigint(column)"
  },
  {
    "title": "DECIMAL",
    "view": "products",
    "column": "price,price.decimal:PriceToDecimal",
    "definition": "Converts column type to DECIMAL",
    "syntax": "column.decimal / decimal(column)"
  },
  {
    "title": "DATE",
    "view": "orders",
    "column": "ctimestamp.date:TimestampToDate,date(\"2025-12-08\"):date",
    "definition": "Converts column type to DATE (column should be of type TIMESTAMP or VARCHAR)",
    "syntax": "column.date / date(value/column)"
  },
  {
    "title": "TIMESTAMP",
    "view": "orders",
    "column": "cdate:cdate,cdate.timestamp:cdateToTs",
    "definition": "Converts column type to Timestamp (column should be of type date or VARCHAR)",
    "syntax": "column.timestamp / timestamp(value/column)"
  }
]