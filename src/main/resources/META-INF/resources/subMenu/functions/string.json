[
  {
    "title": "LENGTH",
    "view": "customers",
    "column": "length(jarvis):basic_length",
    "definition": "Returns the length of a string",
    "syntax": "column.length",
    "examples": [
      {
        "title": "LENGTH with a column",
        "view": "customers",
        "column": "name.length"
      }
    ]
  },
  {
    "title": "CONCAT",
    "view": "customers",
    "column": "concat(Tony, Stark, is also, Iron man)",
    "definition": "Concatenates multiple strings.",
    "syntax": "column.concat(text1,text2,...)",
    "examples": [
      {
        "title": "CONCAT with a column",
        "view": "customers",
        "column": "city.concat(\" can be found in \",country):concat_city"
      }
    ]
  },
  {
    "title": "UPPER",
    "view": "customers",
    "column": "upper(Jarvis):basic_upper",
    "definition": "UPPER(): Converts a string to uppercase.",
    "syntax": "column.upper",
    "examples": [
      {
        "title": "UPPER with a column",
        "view": "customers",
        "column": "name.upper"
      }
    ]
  },
  {
    "title": "LOWER",
    "view": "customers",
    "column": "lower(Jarvis):basic_lower",
    "definition": "LOWER(): Converts a string to lowercase.",
    "syntax": "column.lower",
    "examples": [
      {
        "title": "UPPER with a column",
        "view": "customers",
        "column": "name.upper"
      }
    ]
  },
  {
    "title": "LEFT",
    "view": "customers",
    "column": "country,country.left(3):Left,count:COUNT",
    "definition": "In SQL, the LEFT() function returns the desired number of characters from the first characters of a string. This function will voluntarily truncate the input text to the desired length.",
    "syntax": "column.left(number)"
  },
  {
    "title": "RIGHT",
    "view": "customers",
    "column": "country,country.right(3):Right,count:COUNT",
    "filter": "",
    "definition": "In SQL language, the RIGHT() function allows you to extract the end of a string by defining the desired length. The function allows you to truncate a string starting from the end, by the desired number of characters.",
    "syntax": "column.right(number)"
  },
  {
    "title": "SUBSTRING",
    "view": "customers",
    "column": "name,name.substring(0,4):FirstFourCharacters",
    "filter": "",
    "definition": "SUBSTRING(): Extracts a substring from a string.",
    "syntax": "column.substring(start,end)"
  },
  {
    "title": "REPLACE",
    "view": "products",
    "column": "unit,unit.replace(bottles,bags):replacedUnit",
    "filter": "unit.endsLike=bottles",
    "definition": "Replaces a specified substring within a column with another value.<br>The function searches the column for the given text and substitutes it with the replacement text.<br>This is useful for cleaning or standardizing string values directly in a query.<br><br>Example: in the <b>PRODUCTS_TABLE</b>, the <b>UNIT</b> column may contain values such as '12 bottles'. Using <b>unit.replace(bottles,bags)</b> replaces the word <b>bottles</b> with <b>bags</b>, producing values like '12 bags'.",
    "syntax": "column.replace(replaced,replacement)"
  },
  {
    "title": "REVERSE",
    "view": "categories",
    "column": "reverse(alucard):reverse",
    "definition": "Reverses the characters of a string value.<br>The function returns the text in the opposite order from the original value.<br>For example: <b>'Chai'</b> becomes <b>'iahC'</b>.",
    "syntax": "column.reverse"
  },
  {
    "title": "TRIM",
    "view": "customers",
    "column": "trim(\"            HELLO                             WORLD                       \"):trim",
    "filter": "",
    "definition": "In SQL language, the TRIM() function allows you to remove characters at the beginning and end of a string. Most often, the TRIM() function allows you to remove invisible characters, i.e. characters such as space, tab, line break or even carriage return. Such a function can be useful to save space in a database or to display data properly.",
    "syntax": "column.trim()"
  },
  {
    "title": "LTRIM",
    "view": "customers",
    "column": "ltrim(\"            HELLO                             WORLD                       \"):ltrim",
    "filter": "",
    "definition": "In SQL, the LTRIM() function is used to remove characters from the beginning of a string. The most common use of LTRIM() is to remove invisible characters such as spaces, tabs, and newlines.",
    "syntax": "column.ltrim()",
    "tooltip": "LeftTrim"
  },
  {
    "title": "RTRIM",
    "view": "customers",
    "column": "rtrim(\"            HELLO                             WORLD                       \"):rtrim",
    "filter": "",
    "definition": "In SQL, the RTRIM() function is used to remove characters from the end of a string. The most common use of RTRIM() is to remove invisible characters such as spaces, tabs, and newlines.",
    "syntax": "column.rtrim()",
    "tooltip": "RightTrim"
  },
  {
    "title": "LPAD",
    "view": "products",
    "column": "lpad(arvis,10,J):LPAD",
    "definition": "Pads a string on the left side to a specified length.<br>The function adds the given character to the beginning of the value until the desired length is reached.<br>If the original string is longer than the specified length, it is truncated.<br><br>Example: lpad('Chai', 10, '*') → '******Chai'",
    "syntax": "column.lpad(length, padString)"
  },
  {
    "title": "RPAD",
    "view": "products",
    "column": "rpad(jarvi,10,s):RPAD",
    "definition": "Pads a string on the right side to a specified length.<br>The function adds the given character to the end of the value until the desired length is reached.<br>If the original string is longer than the specified length, it is truncated.<br><br>Example: rpad('Chai', 10, '*') → 'Chai******'",
    "syntax": "column.rpad(length, padString)"
  }
]